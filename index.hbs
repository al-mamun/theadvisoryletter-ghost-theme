{{!< default}}
{{!-- The tag above means: insert everything in this file into the body of the default.hbs template --}}

<main class="gh-main">
    {{!-- showSidebar is off by default: this publication's front page is the
         curated home page, and the publication-info sidebar Source offers here
         duplicates what the footer and about page already say. --}}
    {{> "components/post-list" feed="index" postFeedStyle=@custom.post_feed_style showTitle=true showSidebar=false}}
</main>